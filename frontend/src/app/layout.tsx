// Root layout — just provides basic HTML structure.
// The actual app layout with providers is in [locale]/layout.tsx.
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
